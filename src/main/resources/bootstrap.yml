spring:
  application:
    name: spring-book-service
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev
#这样写自带验证,新版本
management:
  endpoints:
    web:
      exposure:
        include: refresh